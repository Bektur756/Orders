# Running a Django Project using Docker

This guide explains how to set up and run a Django project using Docker containers.

## Prerequisites

Make sure you have the following installed on your system:
- Docker
- Docker Compose
- Git

## Setup

1. **Clone the Repository:**
   ```bash
   git clone https://github.com/Bektur756/Orders.git
   cd orders
   docker-compose up --build

2. **Files to upload should the same format as deals.csv in directory**
   
   
